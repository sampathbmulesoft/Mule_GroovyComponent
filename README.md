# Mule_GroovyComponent

This Mule project uses groovy component to add the two number passed from Postman from with POST call.

url: http://localhost:8081/groovy

POST Call payload is

{
	"a":20,
	"b":33
}
